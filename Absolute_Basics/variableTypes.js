/* Thre primitive types, backbone of all JS Data Structure :
    number
    string
    boolean
*/

// Numbers
let integerCalculation = 1 + 2;  // gives 3
let weirdCalculation = 0.1 + 0.2;  // 0.3 expected, actual answer 0.30000000000000004

// Booleans 
let userIsSignedIn = true;
let userIsAdmin = false;

// Strings
let firstName = "Golam";
let lastName = 'Mafuz';

let wholeName = firstName + ' ' + lastName;  // value: "Will Alexander"
console.log(wholeName)

let anotherwholeName = firstName + lastName; // without space between the strings
console.log(anotherwholeName)